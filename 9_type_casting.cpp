#include<iostream>
using namespace std ;
int main(){
    float integer_variable = 10.5678;
    cout << (int)integer_variable; //explicitly converting the float variable into integer type 

    // In C++ there is a special type casting operator study it.

    return 0;
}